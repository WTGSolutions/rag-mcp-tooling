{
  "name": "@guidetrackee/rag-mcp",
  "version": "0.1.0",
  "description": "Semantic code index (RAG) + MCP server for codebase search",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "rag-index": "./dist/cli/rag-index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0",
    "vitest": "^4.1.8"
  },
  "dependencies": {
    "@huggingface/transformers": "^4.2.0",
    "globby": "^16.2.0",
    "ts-morph": "^28.0.0"
  }
}
